# CBK 项目进度

这张甘特图是**代码**，不是图片。GitHub 会自动把下面这段渲染成一张真的甘特图，
你在网页上看到的就是图。进度变了就改这几行文字，图跟着变，
而且改了什么、谁改的、什么时候改的，git 全都记着。

需求分析说明书里要的「项目规划甘特图」直接截这张图。

> 注意：第 6 周与国庆假期重叠，那一周的产出按半周算比较稳妥。

```mermaid
gantt
    title CBK 数据备份软件 · 十周排期
    dateFormat  YYYY-MM-DD
    axisFormat  %m/%d
    todayMarker stroke-width:2px,stroke:#c0392b

    section 里程碑
    M0 接口冻结（唯一硬依赖）  :milestone, m0, 2026-09-06, 0d
    M1 基础链路 v0.1-base      :milestone, m1, 2026-09-20, 0d
    M2 类型元数据 v0.3         :milestone, m2, 2026-10-04, 0d
    M3 压缩接入 v0.5           :milestone, m3, 2026-10-18, 0d
    M4 加密接入 v0.6           :milestone, m4, 2026-10-25, 0d
    M5 集成与文档 v1.0         :milestone, m5, 2026-11-01, 0d
    M6 答辩                    :milestone, m6, 2026-11-08, 0d

    section Z 基础 / 类型 / 元数据
    接口冻结 · CMake · CI      :active, z1, 2026-08-31, 7d
    基础备份还原                :z2, after z1, 14d
    文件类型 + 元数据           :z3, after z2, 14d
    集成 · 健壮性 · 性能        :z4, after z3, 21d
    文档 · 答辩                 :z5, after z4, 14d

    section 队友A 打包 / 压缩
    格式调研 · 先写测试         :a1, 2026-08-31, 7d
    打包 tar                    :a2, after a1, 21d
    打包 cpio                   :a3, after a2, 14d
    压缩 Huffman                :a4, after a3, 14d
    压缩 LZ77                   :a5, after a4, 7d
    文档 · 答辩                 :a6, after a5, 14d

    section 队友B 加密 / 界面
    PySide6 骨架 · 假 cbk.exe   :b1, 2026-08-31, 7d
    图形界面                    :b2, after b1, 28d
    加密 XOR                    :b3, after b2, 14d
    加密 AES + PBKDF2           :b4, after b3, 14d
    文档 · 答辩                 :b5, after b4, 14d
```

## 怎么读这张图

三条 section 就是三个人，各自的条子是各自的活。**只有 M0 是真正的硬依赖**——
Z 在第一周把接口头文件合进 `main`，A 和 B 才知道要实现什么函数。
之后三条线各走各的，M1 到 M5 是**合并点**：三条分支合进 `main`，
跑一遍完整的备份还原，打一个 tag。

合并点不要求每个人都做完当期的活。A 的 cpio 没写完，M2 照样合，
合进去的是当时能跑的 tar。唯一的要求是：合进 `main` 的东西必须能构建、能过测试。

## 怎么改

用任何文本编辑器打开这个文件改数字就行。几个常用写法：

- `:z2, after z1, 14d` — 接在 z1 后面，持续 14 天
- `:z2, 2026-09-07, 14d` — 指定开始日期
- `:done, z1, ...` — 已完成，会显示成灰色
- `:active, z1, ...` — 进行中，会高亮
- `:crit, z1, ...` — 关键路径，会标红
- `:milestone, m0, 日期, 0d` — 一个菱形的里程碑点

改完 commit 推上去，GitHub 上的图立刻就变了。
